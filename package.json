{
  "name": "printhub",
  "version": "1.0.0",
  "private": true,
  "description": "College print-management system",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "postinstall": "npm run install:all",
    "build": "npm run build --prefix client",
    "start": "npm run build --prefix client && npm run start --prefix server",
    "dev": "npm run build --prefix client && npm run dev --prefix server",
    "dev:watch": "concurrently -n SERVER,CLIENT -c green,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
